[project]
name = "frankfurter-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "elianezanlorense", email = "eliane.zanlorense@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
frankfurter-pipeline = "frankfurter_pipeline:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
