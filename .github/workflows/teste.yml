name: Teste Actions

on:
  push:

jobs:
  teste:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Mostrar mensagem
        run: echo "CI funcionando 🚀"
