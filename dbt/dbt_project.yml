name: 'zoocamp_project'
version: '1.0.0'

profile: 'zoocamp_project'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  zoocamp_project:
    staging:
      +materialized: view
    marts:
      +materialized: table